#include<bits/stdc++.h>
using namespace std;
#define ll long long int

void solve(){
	ll n,i,j,k,m;
	cin>>n;
	vector<string>a(n);
	for(i=0;i<n;i++)
		cin>>a[i];
	for(i=0;i<n;i++){
		
	}
}

int main(){
	ll t,n,a,b;
	cin>>t;
	while(t--)
	{	int j;
		for(j=0;j<10000;j++);
			for(j=0;j<10000;j++);
				for(j=0;j<10000;j++);
					for(j=0;j<10000;j++);
						for(j=0;j<10000;j++);
							for(j=0;j<10000;j++);
		ll arr[7];
		for(int i=0;i<7;i++)
		cin>>arr[i];

		ll sum=arr[6];
		for(int i=0;i<5;i++)
		{
			for(int j=i+1;j<6;j++)
			{
				for(int k=j+1;k<7;k++)
				{
					if(arr[i]+arr[j]+arr[k]==sum)
					{
						cout<<arr[i]<<" "<<arr[j]<<" "<<arr[k]<<endl;
						break;
					}
				}
			}
		}
		int i;
		for(i=0;i<10000;i++);for(i=0;i<10000;i++);for(i=0;i<10000;i++);
	}
	return 0;

}